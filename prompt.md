Context is that I want you to go through the graphs and values attached and then extract a few parameters defined in config.json which will later be used in a matplotlib program to plot the values on a graph resembling a real A4 sized graph sheet.
Do not change the keys of the config.json
I only want you to extract the necessary fields and return the updated config.json